<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.springGroupS.dao.AdminDAO">
	
	<select id="getComplaintList" resultType="com.spring.springGroupS.vo.ComplaintVO">
		select c.partIdx, c.part as part, b.title as title, b.nickName as nickName, c.cpMid as cpMid, c.cpContent as cpContent, date_format(c.cpDate, '%Y-%m-%d %H:%i') as cpDate, c.progress, b.mid as mid, b.content as content from complaint c left join board2 b on c.partIdx=b.idx order by c.idx desc;
	</select>
	
	<select id="getComplaintSearch" resultType="com.spring.springGroupS.vo.ComplaintVO">
		select c.part as part, b.title as title, b.nickName as nickName, c.cpMid as cpMid, c.cpContent as cpContent, date_format(c.cpDate, '%Y-%m-%d %H:%i') as cpDate, c.progress, b.mid as mid, b.content as content from complaint c left join board2 b on c.partIdx=b.idx where c.partIdx=#{partIdx};
	</select>
	
	
	<insert id="setboardComplaintInput">
		insert into complaint values (default,#{vo.part},#{vo.partIdx},#{vo.cpMid},#{vo.cpContent},default,default);
	</insert>
	
	<update id="setMemberLevelChange">
		update member set level = #{level} where idx = #{idx};
	</update>
	
	<update id="setBoardTableComplaintOk">
		update board2 set complaint = 'OK' where idx = #{partIdx};
	</update>
	
	

</mapper>